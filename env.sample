# Copy this file to .env (or set variables in deployment dashboard)
#
# Netlify Identity Configuration
# Get this from your Netlify Dashboard → Site Settings → Identity → JWT Secret

NETLIFY_JWT_SECRET=your_netlify_jwt_secret_here